diff --git a/linux-user/syscall.c b/linux-user/syscall.c
index 1ce4c79..89e9bd0 100644
--- a/linux-user/syscall.c
+++ b/linux-user/syscall.c
@@ -358,7 +358,8 @@ _syscall3(int, sys_sched_getaffinity, pid_t, pid, unsigned int, len,
 #define __NR_sys_sched_setaffinity __NR_sched_setaffinity
 _syscall3(int, sys_sched_setaffinity, pid_t, pid, unsigned int, len,
           unsigned long *, user_mask_ptr);
-/* sched_attr is not defined in glibc */
+/* Newer glibc exposes linux/sched/types.h and its sched_attr definition. */
+#ifndef SCHED_ATTR_SIZE_VER0
 struct sched_attr {
     uint32_t size;
     uint32_t sched_policy;
@@ -371,6 +372,7 @@ struct sched_attr {
     uint32_t sched_util_min;
     uint32_t sched_util_max;
 };
+#endif
 #define __NR_sys_sched_getattr __NR_sched_getattr
 _syscall4(int, sys_sched_getattr, pid_t, pid, struct sched_attr *, attr,
           unsigned int, size, unsigned int, flags);
@@ -2329,6 +2331,27 @@ static abi_long do_setsockopt(int sockfd, int level, int optname,
 #endif
     case TARGET_SOL_SOCKET:
         switch (optname) {
+#if defined(TARGET_RISCV) && TARGET_LONG_BITS == 32
+        /* RV32 glibc uses Linux's time64 socket timeout ABI. */
+        case 66: /* SO_RCVTIMEO_NEW */
+        case 67: /* SO_SNDTIMEO_NEW */
+        {
+            struct target__kernel_sock_timeval *target_tv;
+            struct timeval tv;
+            if (optlen != sizeof(*target_tv)) {
+                return -TARGET_EINVAL;
+            }
+            if (!lock_user_struct(VERIFY_READ, target_tv, optval_addr, 1)) {
+                return -TARGET_EFAULT;
+            }
+            __get_user(tv.tv_sec, &target_tv->tv_sec);
+            __get_user(tv.tv_usec, &target_tv->tv_usec);
+            unlock_user_struct(target_tv, optval_addr, 0);
+            return get_errno(setsockopt(sockfd, SOL_SOCKET,
+                             optname == 66 ? SO_RCVTIMEO : SO_SNDTIMEO,
+                             &tv, sizeof(tv)));
+        }
+#endif
         case TARGET_SO_RCVTIMEO:
         case TARGET_SO_SNDTIMEO:
         {
